const styles = require.requireActual('../styles');

export const SPIN_STYLES = styles.SPIN_STYLES;

export const X_STYLES = styles.X_STYLES;

export const getListStyles = jest.fn(styles.getListStyles);
